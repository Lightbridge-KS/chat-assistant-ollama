# Ollama API Base URL
# This is a build-time environment variable that gets embedded into the static export
#
# Development (local Mac):
# NEXT_PUBLIC_OLLAMA_BASE_URL=http://localhost:11434
# NEXT_PUBLIC_IS_LOCALHOST=true
#
#
# Production (hospital intranet):
# Uses nginx reverse proxy to avoid CORS issues
# NEXT_PUBLIC_OLLAMA_BASE_URL=/api/ollama
# NEXT_PUBLIC_IS_LOCALHOST=false
#
# Note: When IS_LOCALHOST=true, app uses root path and direct Ollama connection
#       When IS_LOCALHOST=false, app uses /radchat basePath and nginx proxy